import { WithZoomableTilesDirective } from './with-zoomable-tiles.directive';

describe('WithZoomableTilesDirective', () => {
  it('should create an instance', () => {
    const directive = new WithZoomableTilesDirective();
    expect(directive).toBeTruthy();
  });
});
